import { QueryMarks } from '../types/QueryManager';

export const defaultPropertiesDivider = ',';
export const defaultStringPropertiesQuote = "'";

export const defaultQueryMarks: QueryMarks = {
  propertiesDivider: defaultPropertiesDivider,
  stringPropertiesQuote: defaultStringPropertiesQuote,
};
